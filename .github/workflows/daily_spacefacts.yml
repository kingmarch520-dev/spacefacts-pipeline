name: Space Facts Automated Pipeline

on:
  schedule:
    # Times below are UTC. Local time is GMT+2 (CAT).
    - cron: "0 16 * * *"   # 18:00 CAT - evening peak begins
    - cron: "0 18 * * *"   # 20:00 CAT - evening peak
    - cron: "30 20 * * *"  # 22:30 CAT - evening peak
    - cron: "0 23 * * *"   # 01:00 CAT - early morning pocket
    - cron: "0 6 * * *"    # 08:00 CAT - morning
    - cron: "0 10 * * *"   # 12:00 CAT - weakest slot, kept for daytime presence
  workflow_dispatch: {}  # Manual trigger option

# Cancels older in-progress runs if a new run is triggered manually
concurrency:
  group: spacefacts-pipeline
  cancel-in-progress: true

jobs:
  generate-and-upload:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install system dependencies & fonts
        run: |
          sudo apt-get update && sudo apt-get install -y ffmpeg fonts-dejavu-core

      - name: Install Python dependencies
        run: |
          pip install google-generativeai edge-tts moviepy pillow requests google-auth-oauthlib

      - name: Run pipeline
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
          YT_CLIENT_ID: ${{ secrets.YT_CLIENT_ID }}
          YT_CLIENT_SECRET: ${{ secrets.YT_CLIENT_SECRET }}
          YT_REFRESH_TOKEN: ${{ secrets.YT_REFRESH_TOKEN }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
        run: python main_spacefacts.py

      - name: Commit updated state file
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add state_spacefacts.json || true
          if ! git diff --staged --quiet; then
            git commit -m "Update state after run [skip ci]"
            git pull --rebase origin main
            git push origin main
          fi
